# Port to listen on (default: 3000)
PORT=3000

# Auth token — leave blank to auto-generate on first run
AUTH_TOKEN=

# Optional: path to an SSH private key for key-based auth
# If not set, the frontend must supply credentials per connection
# SSH_KEY_PATH=/home/user/.ssh/id_ed25519
